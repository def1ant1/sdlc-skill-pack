# Memory System

This module defines the multi-layer memory lifecycle:

- **Episodic memory**: raw execution events.
- **Semantic memory**: promoted stable facts.
- **Organizational memory**: cross-team and cross-workflow durable context.
- **Procedural memory**: reusable execution heuristics and skill improvements.

Primary scripts live in `scripts/memory/`.
