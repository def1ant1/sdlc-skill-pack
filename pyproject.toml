[project]
name = "apotheon-sdlc"
version = "0.1.0"
description = "Advanced SDLC skill platform foundation"
requires-python = ">=3.10"
dependencies = ["pyyaml>=6.0", "pytest>=8.0", "ruff>=0.6.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
